[pytest]
testpaths = tests
# The tools are single-file modules at the repository root. tests/conftest.py puts
# that root on sys.path, rather than restructuring the project to suit the runner.
addopts = -q --strict-markers
